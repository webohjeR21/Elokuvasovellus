/*const pgPool = require('./postgre')

const sql = {
    REGISTER: 'INSERT INTO asiakkaat VALUES (1$, 2$,3$)'
}

async function register(uname, passwdHash, email) {
    await pgPool.query(sql.REGISTER, [uname, passwdHash, email ])
}

module.exports = {register} */